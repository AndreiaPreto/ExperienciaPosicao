import React from 'react';

export const Logo = React.memo(() => (
  <div className="mb-6">
    <h1 className="serif text-4xl font-bold text-gold-light tracking-tight">POSIÇÃO</h1>
  </div>
));

Logo.displayName = 'Logo';
